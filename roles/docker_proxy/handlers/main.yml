---
- name: Reload systemd for Docker proxy
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart active Docker for proxy
  ansible.builtin.systemd_service:
    name: docker.service
    state: restarted
  when: docker_proxy_service_active.stdout | default('') | trim == 'active'
