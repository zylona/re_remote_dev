[defaults]
roles_path = roles
retry_files_enabled = False
host_key_checking = True
interpreter_python = auto_silent
nocows = True
stdout_callback = default
# 执行端可能没有可写的 ~/.ansible；使用系统临时目录，避免 P0 检查依赖用户目录权限。
local_tmp = /tmp

[ssh_connection]
pipelining = True
